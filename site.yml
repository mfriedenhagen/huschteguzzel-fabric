---
- name: Install jenkins, nginx and squid3.
  hosts:
  - jenkins
  - vagrant
  roles:
  - common
  - java
  - jenkins
  - nginx
  - squid3
  - postfix
  post_tasks:
  - lineinfile:
      dest=/etc/motd
      regexp='^MANAGED BY ANSIBLE .*$'
      line='MANAGED BY ANSIBLE rev={{gitinfo_revision}}, branch={{gitinfo_branch}} status={{gitinfo_modified}}'
